{
  "scripts": {
    "postinstall": "npm run build",
    "prestart": "echo 'Starting KGC Healthcare Application'",
    "start": "NODE_ENV=production NODE_OPTIONS='--max-old-space-size=512' node dist/index.js"
  }
}