import React from 'react';
import { render } from '@testing-library/react';
import MainContent from '../patient/dashboard/MainContent';

test('renders MainContent component', () => {
  render(<MainContent />);
});
