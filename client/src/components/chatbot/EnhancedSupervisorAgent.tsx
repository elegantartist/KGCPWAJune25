import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const EnhancedSupervisorAgent: React.FC = () => {
  return (
    <Card>
      <CardHeader><CardTitle>KGC Health Assistant</CardTitle></CardHeader>
      <CardContent><p>Chatbot functionality will be implemented here.</p></CardContent>
    </Card>
  );
};

export default EnhancedSupervisorAgent;